# LearnMath カリキュラム設計書

**作成日:** 2026-01-30
**目的:** 690ユニットの学習順序・依存関係・難易度曲線・コンテンツ品質の統一的な設計

---

## 概要

LearnMathは19カテゴリ・690ユニットの数学学習システムです。本設計では以下を定義します:

- **学習モデル:** ハイブリッド型（構造化 + 自由探索）
- **依存関係:** シンプルチェーン（カテゴリ内1つの前提）
- **強制度:** ソフト推奨（警告のみ、ロックなし）
- **難易度:** 1-10スケール（前提知識の深さベース）
- **コンテンツ:** セクションベース構造 + LaTeX統一表記

---

## 1. Tutorial Data Model

### 1.1 Core Schema

```typescript
interface Tutorial {
  // 基本情報
  id: string;                    // 'group-definition'
  title: string;                 // '群の定義'
  category: string;              // 'algebra'
  description: string;           // 短い説明文

  // カリキュラム構造
  difficulty: number;            // 1-10 (前提知識の深さで決定)
  prerequisite?: string;         // 1つの前提チュートリアルID
  suggestedPrerequisites?: string[]; // 推奨(任意)の前提
  estimatedMinutes: number;      // 学習時間の目安

  // コンテンツセクション
  sections: {
    hook: Section;               // 必須: 導入・動機付け
    concept: Section;            // 必須: 定義・理論
    visualization: Section;      // 必須: 3D視覚化
    examples: Section;           // 必須: 具体例
    exercises?: Section;         // 任意: 練習問題
    summary: Section;            // 必須: まとめ
  };

  // メタデータ
  tags: string[];               // 検索用タグ
  relatedTutorials: string[];   // 関連チュートリアル
  migrationStatus: 'legacy' | 'partial' | 'complete';
}

interface Section {
  content: string;              // MDX形式 (LaTeX数式を含む)
  sceneConfig?: SceneConfig;    // 3Dシーン設定
  interactive?: boolean;        // インタラクション有無
}
```

### 1.2 Design Principles

- **前提は1つまで:** `prerequisite`は単一の必須前提のみ。複数の推奨は`suggestedPrerequisites`
- **カテゴリ独立性:** 三角関数と線形代数は独立。クロスカテゴリ依存は推奨のみ
- **柔軟性重視:** ソフト推奨により自由な学習パスを許容

---

## 2. Difficulty Calculation Algorithm

### 2.1 自動計算ロジック

```typescript
function calculateDifficulty(tutorial: Tutorial, allTutorials: Map<string, Tutorial>): number {
  // ベース難易度: prerequisiteチェーンの深さ
  let depth = 0;
  let current = tutorial.prerequisite;

  while (current) {
    depth++;
    const prereq = allTutorials.get(current);
    current = prereq?.prerequisite;
  }

  // 深さを1-10にマッピング
  const baseDifficulty = Math.min(Math.ceil(depth * 1.5) + 1, 10);

  // 調整ファクター
  const factors = {
    suggestedCount: (tutorial.suggestedPrerequisites?.length || 0) * 0.5,
    hasExercises: tutorial.sections.exercises ? 0.5 : 0,
    categoryBonus: getCategoryBonus(tutorial.category),
  };

  const finalDifficulty = Math.max(1, Math.min(10,
    baseDifficulty + factors.suggestedCount + factors.hasExercises + factors.categoryBonus
  ));

  return Math.round(finalDifficulty);
}

// カテゴリごとの難易度ベースライン
function getCategoryBonus(category: string): number {
  const bonuses = {
    'set-theory': 0,           // 基礎
    'algebra': 1,              // 抽象的
    'linear-algebra': 0.5,     // 視覚的で理解しやすい
    'category-theory': 2,      // 最も抽象的
    'fourier': 1.5,            // 多分野統合
    'topology': 2,             // 高度な抽象化
    'differential-calculus': 0.5,
    'integral-calculus': 1,
    'ode': 1.5,
    'pde': 2,
    'probability': 0.5,
    'statistics': 0.5,
    'graph-theory': 1,
    'discrete-math': 0.5,
    'complex-numbers': 1,
    'exponential': 0,
    'logarithmic': 0.5,
    'trigonometry': 0,
    'laplace': 1.5,
  };
  return bonuses[category] || 0;
}
```

### 2.2 難易度レベルの意味

| Level | 説明 | 前提知識 | 例 |
|-------|------|----------|-----|
| 1-2 | 入門 | 中学数学のみ | 集合の基礎、ベクトル加算 |
| 3-4 | 基礎 | カテゴリ内の基本概念1-2個 | 群の定義、行列の積 |
| 5-6 | 中級 | 複数の基礎概念の組み合わせ | 固有値、フーリエ級数 |
| 7-8 | 応用 | 抽象的思考、多概念統合 | ガロア理論、ラプラス変換 |
| 9-10 | 高度 | 複数分野統合、高度な抽象化 | 圏論、位相空間論 |

---

## 3. Content Quality Standards

### 3.1 数式表現の統一 (LaTeX Conventions)

```typescript
const mathNotation = {
  // 記法ルール
  inline: "$a + b = c$",                    // インライン
  display: "$$\\int_{0}^{\\infty} e^{-x} dx = 1$$",  // ディスプレイ

  // 統一記法
  conventions: {
    vectors: "\\mathbf{v}",                 // 太字: 𝐯
    matrices: "\\mathbf{A}",                // 太字: 𝐀
    sets: "\\mathbb{R}",                    // 黒板太字: ℝ
    operators: "\\operatorname{det}",       // 演算子名
    spaces: "\\,",                          // 薄いスペース
  },

  // 禁止パターン
  avoid: {
    "v⃗": "\\vec{v}を使わず\\mathbf{v}を使用",
    "*": "積は\\cdotまたは\\timesを使用",
    "^": "ハットは\\hatを使用",
  }
};
```

### 3.2 セクション別ガイドライン

#### hook (導入・動機付け)
- **文字数:** 100-200文字
- **目的:** なぜこのトピックが重要か、日常や他分野との関連
- **トーン:** 親しみやすく、好奇心を刺激
- **例:** 「暗号通貨はなぜ安全なのか？その秘密は群論にあります」

#### concept (定義・理論)
- **文字数:** 300-500文字
- **構成:** 直感的説明 → 形式的定義 → 重要な性質
- **数式:** 必須の定義・定理のみ。複雑な証明は避ける
- **例:**
  ```
  群とは、集合Gと演算∗の組(G, ∗)で、以下の4つの公理を満たすもの:
  1. 閉性: ∀a,b∈G, a∗b∈G
  2. 結合法則: ∀a,b,c∈G, (a∗b)∗c = a∗(b∗c)
  ...
  ```

#### visualization (3D視覚化)
- **必須要素:** 3Dシーンまたはインタラクティブ要素
- **説明:** シーンの操作方法 (ドラッグ、クリック等)
- **接続:** 視覚化が概念のどの側面を示すか明記
- **例:** 「3D空間で基底ベクトルをドラッグして線形変換を体感してください」

#### examples (具体例)
- **最小数:** 2個以上
- **バリエーション:** 簡単な例 + 実用的な例
- **フォーマット:** 問題設定 → 解法ステップ → 結果の解釈
- **例:**
  ```
  例1: 整数の加算 (ℤ, +) は群である
  例2: 暗号化での応用: RSAと素数の積
  ```

#### exercises (練習問題) *任意*
- **タイプ:** 選択式、数値入力、インタラクティブ操作
- **難易度:** tutorialのdifficulty ± 1の範囲
- **フィードバック:** 即座の正誤判定 + 解説

#### summary (まとめ)
- **文字数:** 100-150文字
- **内容:**
  - 3つの要点
  - 次に学ぶべきトピックへのリンク
- **例:**
  ```
  まとめ:
  • 群は4つの公理で定義される代数的構造
  • 対称性を記述する数学的言語
  • 暗号・物理・化学で広く応用される

  次へ: 部分群の概念を学びましょう →
  ```

---

## 4. Category Organization

### 4.1 カテゴリメタデータ

```typescript
interface Category {
  id: string;
  name: string;
  description: string;
  icon: string;                    // 3Dアイコンまたは絵文字
  color: string;                   // UI識別用カラー
  order: number;                   // 推奨学習順序(柔軟)
  baseDifficulty: number;          // 1-10: カテゴリ全体の難易度目安
  estimatedHours: number;          // カテゴリ完了の目安時間
  isFoundational: boolean;         // 基礎カテゴリか
}
```

### 4.2 19カテゴリ一覧

| カテゴリ | Units | Difficulty | Hours | Foundational |
|---------|-------|------------|-------|--------------|
| set-theory | 30 | 2 | 8 | ✓ |
| trigonometry | 30 | 3 | 12 | - |
| linear-algebra | 60 | 4 | 25 | ✓ |
| exponential | 30 | 3 | 10 | - |
| logarithmic | 30 | 3 | 10 | - |
| complex-numbers | 30 | 4 | 12 | - |
| differential-calculus | 30 | 5 | 15 | ✓ |
| integral-calculus | 30 | 6 | 18 | ✓ |
| probability | 30 | 4 | 15 | - |
| statistics | 30 | 4 | 15 | - |
| discrete-math | 60 | 5 | 25 | - |
| graph-theory | 60 | 5 | 25 | - |
| algebra | 60 | 6 | 30 | ✓ |
| fourier | 35 | 7 | 20 | - |
| laplace | 30 | 7 | 18 | - |
| ode | 30 | 7 | 20 | - |
| pde | 25 | 8 | 22 | - |
| category-theory | 30 | 8 | 25 | - |
| topology | 30 | 9 | 28 | - |

### 4.3 クロスカテゴリ依存の原則

- **原則:** カテゴリは独立。学習者は任意の順序で学習可能
- **例外:** 高度な統合トピック（フーリエ変換、偏微分方程式等）
- **実装:**
  - カテゴリ内依存: `prerequisite` (必須前提)
  - カテゴリ間依存: `suggestedPrerequisites` (推奨のみ)

```typescript
// カテゴリ内依存 (通常)
{
  id: 'subgroup',
  category: 'algebra',
  prerequisite: 'group-definition',  // 同じカテゴリ内
}

// クロスカテゴリ依存 (例外)
{
  id: 'fourier-transform',
  category: 'fourier',
  prerequisite: 'fourier-series',     // カテゴリ内の主前提
  suggestedPrerequisites: [           // 他カテゴリ (推奨のみ)
    'integral-calculus/definite-integral',
    'complex-numbers/euler-formula',
    'trigonometry/sin-cos-relation'
  ]
}
```

---

## 5. UI/UX Design

### 5.1 ナビゲーションモード

#### モード1: カテゴリグリッド (デフォルト)

```
┌─────────┬─────────┬─────────┐
│ 集合論  │ 三角関数 │ 線形代数│
│ ∈       │ ∿       │ ⟨⟩     │
│ 8/30 ✓  │ 0/30    │ 15/60  │
│ ★★☆☆☆  │ ★★★☆☆  │ ★★★★☆ │
└─────────┴─────────┴─────────┘
```

- 3x7グリッド、カテゴリカード表示
- 進捗、難易度、推定時間を表示
- クリックでカテゴリ詳細へ

#### モード2: ツリービュー (prerequisite可視化)

```
algebra/
├─ ○ closure (2)
├─ ○ group-definition (3)
│  └─ ◌ subgroup (4)
│     └─ ◌ normal-subgroup (5)
│        └─ ◌ quotient-group (6)
└─ ○ symmetry-intro (2)
   └─ ◌ permutation-group (4)
```

- ノードサイズ: 学習時間に比例
- ノード色: 難易度グラデーション (緑→黄→赤)
- アイコン: ✓完了 | ◉進行中 | ○利用可能 | ◌推奨前提あり
- エッジ: 上から下へ (prerequisite → dependent)

#### モード3: 検索・フィルタ

- **フィルタ:** difficulty, category, tags, completion status
- **ソート:** difficulty, recently added, estimated time
- **表示:** リストビュー + プレビュー

### 5.2 個別チュートリアルページ

```
┌────────────────────────────────────────┐
│ Home > Algebra > 群の定義              │
│ ★★★☆☆☆☆☆☆☆ (3/10) | 15分           │
├────────────────────────────────────────┤
│ ⚠ 推奨: 「集合と演算」を先に学習       │
│ [推奨を無視] [前提へ移動]              │
├────────────────────────────────────────┤
│ [hook] [concept] [viz] [ex] [quiz] [sum]│
│              ↑ 現在ここ (3/6)          │
├────────────────────────────────────────┤
│ (コンテンツ表示エリア)                 │
│                                        │
│ [← 前のセクション] [次のセクション →] │
├────────────────────────────────────────┤
│ 次に進む: 部分群 →                     │
│ 関連: 対称群、巡回群                   │
└────────────────────────────────────────┘
```

### 5.3 進捗トラッキング

```typescript
interface UserProgress {
  completed: Set<string>;              // 完了したチュートリアルID
  inProgress: Map<string, number>;     // ID → セクション進捗 (0-6)
  timeSpent: Map<string, number>;      // ID → 学習時間(分)

  stats: {
    totalCompleted: number,            // 45/690
    byCategory: Map<string, number>,   // algebra: 8/60
    averageDifficulty: number,         // 平均4.2
    totalHours: number,                // 累計18.5時間
    streak: number                     // 連続7日
  };
}
```

---

## 6. Migration Strategy

### 6.1 現状と課題

**現在の構造:**
```typescript
interface CurrentTutorial {
  id: string;
  title: string;
  description: string;
  category: string;
  steps: Step[];  // 自由形式のstep配列
}
```

**必要な移行:**
- `difficulty`の追加（自動計算可能）
- `prerequisite`の設定（手動レビュー必要）
- `estimatedMinutes`の追加（steps数から推定）
- `steps` → `sections`への再構成
- LaTeX数式の統一

### 6.2 移行フェーズ

#### Phase 1: スキーマ拡張 (後方互換性維持)

```typescript
interface TransitionTutorial {
  // 既存フィールド (そのまま維持)
  id: string;
  title: string;
  description: string;
  category: string;
  steps?: Step[];                    // 旧形式 (オプショナル)

  // 新フィールド (段階的に追加)
  difficulty?: number;
  prerequisite?: string;
  estimatedMinutes?: number;
  sections?: TutorialSections;       // 新形式 (オプショナル)
  tags?: string[];

  // マイグレーション状態
  migrationStatus: 'legacy' | 'partial' | 'complete';
}
```

#### Phase 2: 自動移行スクリプト

```typescript
async function migrateToNewSchema(tutorial: CurrentTutorial): Promise<TransitionTutorial> {
  return {
    ...tutorial,

    // 自動計算
    difficulty: calculateDifficultyFromSteps(tutorial),
    estimatedMinutes: tutorial.steps.length * 3,  // 1step=3分

    // AI支援でセクション分類
    sections: classifyStepsIntoSections(tutorial.steps),

    // 自動タグ生成
    tags: extractTagsFromContent(tutorial),

    // 手動設定が必要
    prerequisite: undefined,  // 要レビュー

    migrationStatus: 'partial'
  };
}
```

#### Phase 3: 手動レビューツール

- Side-by-side比較 (旧 vs 新)
- prerequisite設定UI
- difficulty調整
- セクション再分類
- バッチ操作（カテゴリ全体に適用）

### 6.3 移行優先順位

| Phase | Categories | Units | 期間 | 目的 |
|-------|-----------|-------|------|------|
| Week 1 | set-theory, trigonometry | 60 | 1週 | 基礎カテゴリで新UIテスト |
| Week 2 | linear-algebra, exponential, logarithmic | 120 | 1週 | ベータユーザーフィードバック |
| Week 3 | calculus, probability, statistics | 150 | 1週 | prerequisite調整 |
| Week 4 | 残り全カテゴリ | 360 | 1週 | 全移行完了 |

### 6.4 検証チェックリスト

各チュートリアル移行時:
- [ ] difficulty が妥当 (prerequisiteチェーンと一致)
- [ ] prerequisite が設定済み (必要な場合)
- [ ] 全6セクションが適切に分類
- [ ] LaTeX数式が統一記法
- [ ] tags が内容を反映
- [ ] estimatedMinutes が現実的
- [ ] 3D視覚化が動作

---

## 7. Implementation Roadmap

### 7.1 フェーズ1: データモデル実装 (Week 1)

**タスク:**
1. `packages/tutorials/src/types.ts`を新スキーマに更新
2. 後方互換性のための`TransitionTutorial`型追加
3. `calculateDifficulty()`関数実装
4. カテゴリメタデータ定義

**成果物:**
- 型定義ファイル
- 難易度計算ロジック
- カテゴリ設定JSON

### 7.2 フェーズ2: 移行ツール開発 (Week 2)

**タスク:**
1. 自動移行スクリプト作成
2. セクション分類AI (GPT-4使用)
3. 手動レビューUI構築
4. バッチ操作機能

**成果物:**
- `scripts/migrate-tutorials.ts`
- レビューWebツール

### 7.3 フェーズ3: UI実装 (Week 3-4)

**タスク:**
1. カテゴリグリッドビュー
2. ツリービュー (React Flow使用)
3. 検索・フィルタ機能
4. 進捗トラッキング
5. prerequisite警告システム

**成果物:**
- 3つのナビゲーションモード
- チュートリアルページUI
- ダッシュボード

### 7.4 フェーズ4: コンテンツ移行 (Week 5-8)

**タスク:**
1. Week 1-4の優先順位に従って690ユニット移行
2. 各カテゴリでprerequisiteチェーン検証
3. ベータテストとフィードバック収集
4. LaTeX数式の統一

**成果物:**
- 690ユニット完全移行
- カテゴリごとの学習パス
- ドキュメント

---

## 8. Success Metrics

### 8.1 技術指標

- ✓ 690ユニット全てが新スキーマに移行
- ✓ prerequisiteチェーンに循環依存なし
- ✓ 難易度分布が適切 (各レベルに適切な数)
- ✓ LaTeX数式の統一率 100%
- ✓ 全チュートリアルに6セクション

### 8.2 学習体験指標

- ユーザーの平均完了率 > 30%
- prerequisite警告の無視率 < 20% (適切な推奨)
- セッション時間 > 15分/回 (エンゲージメント)
- カテゴリ間の移動率 > 40% (探索性)

---

## 9. Next Steps

1. ✅ **設計承認** (完了)
2. **実装準備:**
   - Git worktreeで隔離された開発環境作成
   - 詳細な実装計画書作成
3. **開発開始:**
   - Phase 1から順次実装
   - 週次レビュー・調整

**推奨:** `superpowers:using-git-worktrees` + `superpowers:writing-plans` スキルで実装開始

---

**Document Status:** ✅ Approved
**Next Action:** Implementation planning
